//Funciones que se utilizan a menudo en cuando a los jugadores
'use strict'

module.exports = {
    validate : (team)=>{ 
        // After validation;
        console.log(team)
        return true;
    }
} 