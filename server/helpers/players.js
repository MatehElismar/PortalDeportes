//Funciones que se utilizan a menudo en cuando a los jugadores
'use strict'

module.exports = {
    validate : (player)=>{
        // After validation;
        console.log(player)
        return true;
    }
}